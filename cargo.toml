[package]
name = "rust-cli-spotify"
version = "0.1.0"
authors = ["Dev Menon"]
edition = "2018"

[dependencies]
structopt = "0.3.22"
rspotify = { version = "0.10.0", features=["blocking"]}
futures = "0.3"
